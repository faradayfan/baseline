{{- if not .Values.secrets.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "baseline.secretName" . }}
  labels:
    {{- include "baseline.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: {{ include "baseline.postgresDSN" . | quote }}
  {{- if .Values.mem0.enabled }}
  MEM0_URL: {{ printf "http://%s-mem0-api:8000" .Release.Name | quote }}
  MEM0_API_KEY: {{ .Values.mem0.apiKey | quote }}
  {{- end }}
{{- end }}
