apiVersion: v2
name: baseline
description: >-
  Baseline — facts-management & governance service (with bundled pgvector
  Postgres).
type: application
version: 0.2.1
appVersion: 0.2.1
dependencies:
  - name: postgresql
    version: 16.x
    repository: https://charts.bitnami.com/bitnami
    alias: postgres
    condition: postgres.enabled
